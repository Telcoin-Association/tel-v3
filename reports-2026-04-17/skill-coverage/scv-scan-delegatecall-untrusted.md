# kadenzipfel/scv-scan#delegatecall-untrusted-callee — Coverage

**Checked — not vulnerable because:**

`grep -rn "delegatecall" src/` returns no hits in the in-scope code. The project is not upgradeable and uses no proxy patterns.
